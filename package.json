{
  "name": "paintr",
  "main": "server.js",
  "dependencies": {
    "express" : "~4.5.1",
    "serve-favicon": "*",
    "bower": "*"
  },
  "scripts": {
    "postinstall": "bower cache clean && cd public && bower install"
  }
}

